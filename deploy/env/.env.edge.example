DOMAIN=your-subdomain.duckdns.org
EMAIL=your-email@example.com

# Optional DuckDNS updater container settings for profile `duckdns`.
# SUBDOMAINS is the left side only (without .duckdns.org).
SUBDOMAINS=your-subdomain
TOKEN=your-duckdns-token

# Optional certbot renew interval in seconds (default 43200 = 12h).
CERTBOT_RENEW_INTERVAL_SECONDS=43200
